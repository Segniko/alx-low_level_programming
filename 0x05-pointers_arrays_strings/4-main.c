#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int main(void)
{
	char *str;
	
	str = "I do not fear the computers. I fear the lack of them - Issac Asmiov";
	print_rev(str);
    return (0);
}
